@model Course

<div class="card">
    <img src="~/Screenshot_1.png" class="card-img-top img-fluid" alt="...">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        <p class="card-text">@Model.Description</p>
        <a asp-area="" asp-controller="Home" asp-action="Course" asp-route-id=@Model.Id class="btn btn-primary">Пройти</a>
    </div>
</div>